# ITfix Production Environment Variables
# Copy to .env.production and fill in values

# Site URL (used for metadata, sitemap, canonical URLs)
NEXT_PUBLIC_SITE_URL=https://itfix.systems

# Node environment
NODE_ENV=production

# Server settings
PORT=3000
HOSTNAME=0.0.0.0
